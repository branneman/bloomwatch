# CLAUDE.md

Guidance for Claude Code (and other agents) working in this repo.

## Project

Bloomwatch is a static, backend-less web app that turns a Warcraft Logs report into a process-quality scorecard for TBC Resto Druids. In scope: TBC content only — Anniversary ("fresh") realms and the original 2021-2024 Classic-launch TBC window (`classic.warcraftlogs.com`) — no other WoW version, expansion, or realm type. Full vision, principles, and phased roadmap: `docs/roadmap.md`. Full backlog of user stories (the unit of implementation work): `docs/backlog.md`. Testing strategy and tooling: `docs/testing.md`.

Read both docs before starting substantial feature work — they define scope, thresholds, and acceptance criteria that shouldn't be re-derived or guessed. Always read `docs/testing.md` before writing an implementation plan — it defines which test tier each kind of change belongs in and the tooling/conventions (factories, fixtures, co-location) each tier expects, none of which should be re-derived or guessed either.

## Product principles (do not violate silently)

1. **Process over output.** Never add a metric based on HPS, effective healing rank, or parse percentile.
2. **No backend.** No server-side code, no database, no accounts. All WCL API calls happen client-side; all computation happens in the browser.
3. **Judgement is visible and sourced.** Every Good/Fair/Bad threshold must be documented, with a comment pointing to its rationale in `docs/backlog.md` — don't hardcode one without it. Per story 802, thresholds are periodically recalibrated by the maintainers (not the end user) once all metrics exist; there is no user-facing threshold-editing UI planned.
4. **FOSS.** Keep the repo reproducible from a clean clone with no required secrets at build time.
5. **Internal language stays internal.** Never expose developer/process vocabulary — "epic", "story"/"stories", "backlog", story or epic numbers, and similar planning-doc terms — in anything a user sees in the running app: labels, headings, tooltips, methodology copy, error messages, aria-labels/titles. That vocabulary belongs only in `docs/`, code comments, identifiers, and commit messages. When referencing another metric/section from user-facing copy, name it by its on-screen label (e.g. "Death forensics") instead.
6. **No em dashes in user-facing text.** Don't use the em dash character ("—") in anything a user sees in the running app: labels, headings, tooltips, methodology copy, error messages. Use a comma, colon, semicolon, parentheses, a separate sentence, or (for compact label-joining, e.g. `${name} · ${role}`) the existing "·" separator convention — whichever reads most naturally for that spot. Docs, code comments, and commit messages are unaffected.

## Working conventions

- **Commits follow [Conventional Commits](https://www.conventionalcommits.org/)**: `type(scope): summary`, e.g. `feat(lifebloom): add LB3 uptime per target`, `fix(auth): handle expired PKCE token`, `docs: update roadmap`. Common types: `feat`, `fix`, `docs`, `refactor`, `test`, `chore`, `ci`. Scope is optional but preferred when a change is confined to one epic/module (e.g. `gcd`, `lifebloom`, `mana`, `auth`, `wcl-client`).
- Backlog stories (`docs/backlog.md`) are the primary unit of work — one story is intended to be independently implementable in one session. When implementing a story, its acceptance criteria are the spec; don't add scope beyond them.
- Spell/ability IDs must never be hardcoded — resolve them from the report's `masterData.abilities` at runtime (see backlog story 007). TBC has multiple ranks per spell.
- `src/metrics/*.ts`'s `compute*` functions have two independent consumers: the UI's metric cards/`Scorecard/use*Summary.ts` hooks, and `scripts/calibrate.ts`. When changing a `compute*` function's signature, check both — `npm run typecheck` (which covers `scripts/` via `tsconfig.scripts.json`, wired into pre-commit and CI) catches type-level signature mismatches immediately. The real risk is a same-typed argument-order swap (e.g. two adjacent `number` parameters reordered), which type-checks fine and only surfaces as a wrong computed value at runtime.
- No secrets should ever be required at build or deploy time (see story 801 / principle 2). If an auth approach needs a client secret, it does not meet the no-backend bar — flag it rather than working around it.
- Design specs go in `docs/specs/<topic>-design.md`; implementation plans go in `docs/plans/<topic>-plan.md`. No `superpowers` subdirectory, no dates in filenames. Once a story ships and its lasting details (rationale, tooling, conventions) are captured in a permanent doc (`docs/testing.md`, `docs/wcl-auth.md`, etc.), the spec/plan can be deleted — grep the repo for the file path first to confirm nothing else references it.
- **A story isn't done until its paperwork is retired.** The moment a story ships, mark it `✅ Done` in `docs/backlog.md` and delete its `docs/specs/*-design.md` / `docs/plans/*-plan.md` files in the same commit (fix any dangling references first, per the point above). This is the standard for every story going forward — don't leave completed specs/plans lying around for a later cleanup pass.
- Static analysis (typecheck, ESLint, Prettier) runs full-project — via a pre-commit hook and in CI, per `docs/testing.md` — not scoped to changed files only. Don't bypass the pre-commit hook.
- **Merging branches: fast-forward only.** Never create a merge commit (`git merge --no-ff`, or a merge commit from a PR's "Merge" button). Integrate branches via rebase (`git rebase`) followed by a fast-forward merge (`git merge --ff-only`), or a rebase-and-merge / squash-and-merge on GitHub. Keeps history linear.

## Running live WCL queries yourself

Prefer `scripts/wcl-query.ts` over raw curl — it handles auth and host selection for you:

```bash
npm run wcl:query -- 'query { reportData { report(code: "4GYHZRdtL3bvhpc8") { title fights { id name startTime endTime } } } }'
```

Pass `--host classic` or `--host fresh` to hit `classic.warcraftlogs.com`/`fresh.warcraftlogs.com` instead of the default `www.warcraftlogs.com` — useful since these three hosts don't always serve the same data for the same zone/report (e.g. `classic.warcraftlogs.com`'s SSC/TK zone conflates the 2021 TBC Classic launch with Anniversary data; `www.warcraftlogs.com` has a separate, Anniversary-only zone for the same content). See `scripts/wcl-query.ts` for the full `--host`/`--file` options.

For calibrating the app's actual judgement output against a real report (every metric, every fight, with numeric pooling across the whole report), use `npm run calibrate -- <reportCode>` instead — see `docs/testing.md` for what it produces.

**Fallback (no Node available):** raw curl still works the same way it always did:

```bash
set -a; source .env.local; set +a
curl -s -X POST https://www.warcraftlogs.com/api/v2/user \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $WCL_TEST_ACCESS_TOKEN" \
  -d '{"query":"query { reportData { report(code: \"4GYHZRdtL3bvhpc8\") { title fights { id name startTime endTime } } } }"}' \
  -o /path/to/scratch/response.json
```

Use this to capture real Tier 2 fixtures (`test/integration/fixtures/*.json`) or spot-check a field's real shape/scale before writing code against an assumption. `docs/testing.md`'s "Known real test reports" table lists report codes already validated against for this purpose (with what each is notable for) — check there before reaching for a new report, and add to it when a new one earns its place. Keep the token out of logs and chat output:

- Reference it only as `$WCL_TEST_ACCESS_TOKEN` in commands (never inline the literal value).
- Never `cat`/`echo`/print `.env.local` or the token itself.
- Redirect responses to a file (`-o`) rather than letting curl dump to stdout, especially if a header-echo flag (`-v`, `-i`) is ever added.
- `.env.local` is gitignored — never `git add` it.

## Repo state

Phase 0 (WCL auth pipeline, `docs/wcl-auth.md`), story 801 (build & test tooling foundation — Vite + React + TypeScript, full test pyramid, CI/CD to GitHub Pages, `docs/testing.md`), story 002 (report URL/code input), story 003 (fight list & selection), story 004 (zone-wide selection — its spec has since been removed from `docs/backlog.md`, superseded by the whole-report dashboard in 702; the old multi-select `FightPicker` code has since been deleted and replaced by 702's dashboard), story 005 (druid auto-detection & selection), story 006 (event fetching & caching layer), story 007 (ability resolution table), story 101 (active time & GCD utilization), story 102 (idle-gap detection), story 201 (LB3 uptime per target), story 202 (refresh cadence histogram), story 203 (accidental bloom counter), story 204 (re-stack tax), story 205 (concurrent LB3 targets), story 701 (single-fight scorecard), story 008 (default API client fallback), and story 301 (HoT clip detection) are complete and live — Phase 1 MVP is done. Epic D (stories 301, 302, 303, and 304) is fully done. Epic E (mana economy) has stories 401 (mana curve & ending mana), 402 (consumable throughput), 403 (Innervate audit), and 404 (HoT-aware overheal table) done — 403 rewards handing Innervate to a mana-using ally rather than assuming self-cast is the goal, since druids have strong natural mana regen from Spirit; see docs/backlog.md story 403 for the full Good/Fair/Bad shape. Story 501 (per-death resource audit, epic F) is done, implemented out of its suggested order since it was requested directly. Story 601 (pull-time consumables check, epic G) is also done — free-floating per `docs/backlog.md`'s ordering note, implemented ahead of the remainder of epic E. Story 702 (whole-report dashboard, epic H) is done too — it's now the primary screen a user lands on after druid selection (005), aggregating every non-trash fight in the report via its own per-boss list (which now fulfills 003's former role rather than a separate fight-picker screen), with each row drilling into that fight's single-fight scorecard (701). Story 705 (onboarding screen, epic H) is also done — free-floating with no dependency on any metric epic. Story 703 (shareable report state, epic H) is done too — the URL hash is now the single source of truth for navigation through report/druid/fight/epic selection, and the browser's back/forward buttons work throughout the flow. Story 707 (Good/Fair/Bad judgement labels, epic H) is done too, implemented out of its suggested order (ahead of 703/010) since it was requested directly — every judgement chip renders through the single `JudgementChip` component, so the label sweep was a one-file change (`src/app/components/ui/JudgementChip/index.tsx`) plus updating downstream tests that asserted the old color-name text. Story 010 (WCL request performance & loading-state audit, epic A) is done too, also implemented out of its suggested order (ahead of 703) since it was requested directly — the audit found two categories of avoidable requests (both now fixed): two unused fields (`gameZone` on fights, `icon`/`type` on masterData abilities) padding out `src/wcl/client.ts` queries, and a silent event-cache split where some call sites requested `includeResources: true` for `Casts`/`Healing` events and others omitted it (defaulting `false`), which are two different cache keys for conceptually the same data — every such call site now passes `true` consistently (see the comment in `src/wcl/eventCache.ts`), collapsing what was 2 requests per fight per dataType into 1. The eager whole-report per-fight fetch in `ReportDashboard` was reviewed and left as-is with a justifying comment (it's required for the aggregate chip strip, not an oversight). Every WCL-triggering screen already had a loading state under the app's existing plain-text convention (`Calculating…`, `Loading report…`, etc.), so no new UI work was needed there. Story 011 (Dreamstate-spec test coverage, epic A) is done too, free-floating per its own ordering note — `docs/testing.md`'s known-test-reports table gained a new entry (report `bKRZ68XqgwYkxtzm`, a different guild than every other row) documenting a real Dreamstate-spec (35/0/26) druid confirmed via WCL's own `Druid-Dreamstate` icon and a `CombatantInfo` event's raw talent points, and `druidDetection.test.ts` now asserts detection behavior against that real captured data. A Swiftmend-availability gap in 302's audit and 501's death forensics — both currently can't distinguish "talent unreachable" from "available but unused" — was flagged in `docs/backlog.md` rather than fixed, since a proper fix needs talent-point resolution the app doesn't have anywhere today. Story 009 (rate-limit usage banner, epic A) is done too — every WCL query now requests `rateLimitData { limitPerHour pointsSpentThisHour }` alongside its existing fields, and the shared low-level `postGraphQLOnce` (`src/wcl/client.ts`) broadcasts it through a small subscribe/publish module (`src/wcl/rateLimitUsage.ts`) rather than a callback threaded through every fetch function like 008's `withRateLimitDetection` — a deliberate choice since usage % is a continuously-updating ambient fact, not a per-request event tied to one call site. `App.tsx` renders the resulting banner as true app-wide chrome (a sibling to every screen block) once usage crosses 75% on the shared default client, hidden whenever 008's blocking 429 fallback is already showing or the user has supplied their own Client ID. Story 708 (global error handling & recovery overlay, epic H) is done too — a class-based `ErrorBoundary` (`src/app/components/ErrorBoundary`) wraps `<App/>` in `main.tsx` for uncaught render errors, and `App.tsx` itself catches any other unexpected WCL/auth failure via a `reportError` callback threaded through `withErrorReporting` (`src/wcl/client.ts`, layered outside `withRateLimitDetection` so a 429 is filtered before it ever reaches `reportError`) and into `useWclAuth`; both paths render the same `ErrorOverlay` (apology, collapsed-by-default "View details" with the error's message/stack/timestamp, a "Start over" button that resets the URL hash and reloads, and a GitHub-issues link). Every WCL request also gets a 30s timeout via a shared `fetchWithTimeout` helper, composed underneath the existing `postGraphQL` GraphQL-retry helper from the unrelated concurrent `fix(wcl-client)` work. The ~20 components that fetch WCL data directly (17 metric cards, `ConnectPanel`, `DruidDetector`, `AbilityResolver`) each split their fetch-stage failures (which now escalate to the overlay, since a WCL fetch failure typically means the whole screen is unusable) from their compute-stage failures (a bug in one metric's own calculation, which stays a local inline error — the one case where per-widget isolation still helps more than a full-screen takeover). The rate-limit banner (008/009) is the sole deliberate exception to this "one error path" rule, kept exactly as before. Story 706 (responsive/mobile layout, epic H) is done too — a Claude Design pass (`docs/design_v6`, gitignored like `design_v3`/`design_v4`/`design_v5`) produced the mobile-first spec, whose `responsive-design.md` is now the permanent `docs/responsive-design.md` (the breakpoint table, three reflow archetypes, typography, and touch/input rules referenced by every screen going forward). The core bug it fixed: `Shell` (the app's one bordered-column container, used by every screen) was setting its width as a literal inline pixel style per call site (`style={{width: 820}}` etc.), which overflowed horizontally on any viewport narrower than that configured value — it's now fluid, capped at `--content-max: 1126px`, and its `width` prop is gone entirely, so every screen shares one consistent shell. `Input`'s font bumped to 16px (iOS Safari auto-zoom fix), every standalone interactive element (buttons, the Disclosure toggle, DruidPicker's radio rows, back-links) got a 44px touch target except inline links embedded in a sentence of prose (WCAG 2.5.5's own exemption — `App.module.css`'s `.aboutLink` and `Scorecard`'s "View on Warcraft Logs →" link were deliberately left alone). The three reflow archetypes from `docs/responsive-design.md` were applied at their exact 600/768/1024/1280px mobile-first breakpoints: form (`ReportInput`'s field+button row-merge from 600px; `OwnClientIdField`'s button-only fix, since an explanatory paragraph sits between its field and button so a full row-merge would look wrong there), widget grid (`Scorecard`'s single-fight overview and `ReportDashboard`'s aggregate chip strip both now an explicit 1→2→3 column grid, replacing an `auto-fit`/`minmax` trick and a plain flex-wrap strip respectively — never `auto-fit`), and metric row (`MetricCard`/`Widget` headers and `ReportDashboard`'s boss-list rows now `flex-wrap` instead of overflowing on narrow cards). `DataTable` (used by 5 different per-epic cards) gained a shared `overflow-x: auto` wrapper so wide tables scroll within their own card instead of breaking the page. 706 ran concurrently with 009 and 708 in separate worktrees and didn't sweep either's UI for responsiveness since neither existed on its branch — both have since merged, so that check is open as a small follow-up if it turns out to matter. The per-epic cards' dense timestamp deep-links (e.g. `RestackTaxCard`, `IdleGapsCard`, `AccidentalBloomsCard`, `HotClipDetectionCard`) were deliberately left without a 44px touch target — they're citation/evidence links in a list of many, not primary flow controls, closer to the WCAG 2.5.5 inline-link exemption already used for `.aboutLink` and Scorecard's WCL link than to a real "list row" per `docs/responsive-design.md` §5. Story 012 (support `classic.warcraftlogs.com` TBC reports for subscribed users, epic A) is done too — `parseReportInput` now accepts `classic.` links alongside `fresh.`, and a report's TBC-content and access checks (`Report.zone.expansion.id` / `Report.archiveStatus.isAccessible`, both added to the existing `fetchReportFights` query at no extra request cost) happen in `ConnectPanel` after the fetch resolves, since they need the network round trip `parseReportInput` itself doesn't make. Live testing during this story found report codes resolve identically regardless of which of `www`/`classic`/`fresh` WCL API host serves the request, so `src/wcl/client.ts`'s API base URL needed no per-host routing — the parsed `host` is used only for building outbound deep-links back to WCL's own web UI (threaded through the URL hash, per 703, and down through every metric card that links out). Story 903a (per-fight talent-archetype detection, epic I) is done too — a shared `src/report/archetypeDetection.ts` module (extracted from story 900's `scripts/tagArchetypes.ts`, so both the CLI tool and the app now use the same classifier) feeds a new `useArchetypeBucket` hook, which surfaces the detected bucket per fight directly in the Scorecard header. Story 903b (per-fight healing-role detection, epic I) is done too — it retires story 709, whose off-role-fight exclusion is now a special case of `detectHealingRoleThisFight`'s general per-fight mechanism (reusing story 005's `HEALING_SPELL_NAMES`/`MIN_HEALING_CASTS_FOR_DETECTION` unchanged, just applied per fight instead of once per report); an off-role fight is excluded from 702's aggregate rollup, labeled "Not healing this fight" in the fight list, and gets a matching caveat in the single-fight Scorecard. Story 709's own backlog entry has been removed entirely, per the same precedent already used for story 004. Story 903d (onboarding notice on supported playstyles, epic I) is done too — the onboarding screen (705) gained a "Which builds this fits" section naming which archetypes are well-supported in generic terms, and `Scorecard` (which already surfaces 903a's detected bucket per fight) now also renders a warning `Alert` when that bucket is `mostly-resto`, `mostly-balance`, `other-unclassified`, or the currently-unreachable `restokin-shaped` — `likely-dreamstate-full`/`-partial` are deliberately left unflagged since the backlog calls Dreamstate "supported to a lesser extent," and `unknown-no-talent-data` is left unflagged too since a failed talent read can't honestly be called either supported or unsupported. Story 904 (overhaul whole-report rollup policy, epic I) is done too — the whole-report dashboard's per-epic verdict is now a duration-weighted median across that epic's fights (`weightedMedianJudgement` in `src/metrics/judgement.ts`) instead of a strict worst-of, with a fight-count breakdown rendered alongside each chip so a single bad pull no longer silently dominates the headline while still being visible; the same policy is shared by `scripts/lib/rollup.ts`'s CLI calibration rollup via the same function. Story 906 (fix locale-dependent ability-name matching, epic A) is done too — `src/report/druidDetection.ts`'s `detectDruids` (which decides who counts as a healer at all) now matches each ability's game ID against `resolveAbilities.ts`'s `SPELL_RANKS` table first (locale-independent, since game IDs never change across client languages and `SPELL_RANKS` already has complete TBC rank coverage for every multi-rank healing spell), falling back to a new shared `src/abilities/localizedSpellNames.ts` lookup only for unranked/unrecognized IDs — the same fallback `resolveAbilities.ts`'s own existing name-matching path was refactored to use. Dark Rune and Demonic Rune moved to gameID-only matching (real IDs confirmed live: 27869, 16666), the same treatment Mana Potion already had, eliminating their name-matching fallback entirely. `localizedSpellNames.ts` is populated for all 10 Blizzard-supported client languages; none are validated against a real non-English-logged WCL report (none has been found — a report reflects the uploader's client language, not each player's own), so all are sourced from reference material, documented per-language in `docs/testing.md`'s "Locale coverage" section. That research surfaced a genuine data-quality trap worth remembering: wowhead's TBC-Classic-scoped Italian pages showed several spell names identical to English, which turned out to be a wowhead classic-page gap rather than real in-game data — cross-checking against Blizzard's own official Italian patch notes and wowhead's _retail_ Italian pages found real, distinct Italian names for 7 of 8 spells (the exception, Nature's Swiftness, has a Druid-specific gameID that 404s on current retail wowhead, suggesting Blizzard reworked or reassigned it, so that entry is community-guide-sourced only). Story 1001 (near-death response audit, epic J) is done too — a new "Crisis response" card audits raid-wide near-death moments (a target's HP dropping to <=15%, provisional, and surviving) the same way story 501 audits actual deaths, reusing its exact readiness machinery (Swiftmend/Nature's Swiftness/idle-GCD tally) for the unspent-resource judgement, with a scope exemption (crises outside a druid's clear 1-2 target tank assignment are shown as context only, not judged) that 501 itself doesn't need since it only ever judges maintained-target deaths. Story 802 (threshold calibration pass, epic A) is marked done too — its own acceptance criteria (threshold inventory, calibration tooling) were already satisfied, and Epic I is the doc-of-record for how that calibration work actually continued; its now-stale spec/plan (`docs/specs/calibration-tooling-design.md`, `docs/plans/calibration-tooling-plan.md`) were deleted in the same pass, and story 704 (Markdown export, epic H) was removed from the backlog entirely at the user's request (never implemented, no longer wanted) — 707's and CLAUDE.md's own dangling references to it were fixed rather than left stale, per the same precedent as story 004's removal. Also swept out this session: every remaining `red`/`orange`/`green`/"R/O/G" reference across `src/`, `scripts/`, and the still-open Epic I backlog stories (909-914) — the `Judgement` type (`src/metrics/judgement.ts`) and every threshold field name derived from it (`greenMin`/`orangeMin` etc.) are now `"good"`/`"fair"`/`"bad"` throughout, cascading automatically through the CSS module classnames keyed off that type (`JudgementChip`, `ProgressBar`) since `styles[judgement]` picks up the renamed values for free; the underlying `--judgement-green/-orange/-red` CSS custom properties in `src/index.css` were renamed to `--judgement-good/-fair/-bad` (same hex colors, new token names) since `Badge`/`ClassTag`'s own tone names were unaffected. `docs/thresholds.md`, `docs/roadmap.md`, and `docs/testing.md` were updated to match, `✅ Done` backlog story text was deliberately left as the original historical record (matching how git history isn't rewritten), and the whole-report rollup's `weightedMedianJudgement` (`src/metrics/judgement.ts`) gained a new rule requested directly: an epic with at least one good fight and at least one bad fight now reads "fair" outright, ahead of the median calculation — deliberately symmetric (a mostly-good epic with one bad outlier is capped at "fair" too, not just a mostly-bad epic with one good outlier), documented in `docs/thresholds.md`'s compounding-factors section. A follow-up refinement the same day extended that mixed-good-and-bad-reads-fair rule one level deeper: `epicSummary.ts`'s 4 multi-metric functions (GCD Economy, Lifebloom Discipline, Spell Discipline, Mana Economy) plus `prepHygiene.ts`'s `computePrepHygiene` (Prep Hygiene) — 5 functions total, across 2 files — now use a new `mixedJudgement` (`src/metrics/judgement.ts`) instead of strict `worstJudgement` when combining their own sibling metrics into one fight's epic verdict — a good+bad mix now reads "fair" there too, falling back to worst-of otherwise (a fair-only mix with no bad present is deliberately unaffected, unlike a full majority-by-count median which was considered and rejected — see `docs/thresholds.md`'s compounding-factors section). A follow-up threshold-recalibration pass the same week, requested directly (not a new corpus calibration), revised LB3 uptime per target's good/fair bands 90%/75% → 80%/60%, and — motivated by a real fight where 3 well-maintained targets (96%/75%/94% uptime) used to read "fair" under flat worst-of — changed how per-target LB3 judgements fold into the Lifebloom Discipline epic verdict: they're now reduced to one representative judgement via `weightedMedianJudgement` (weighted by each target's own tracked-uptime window) before joining the epic's other siblings, rather than every target being flattened in alongside them. The same pass resolved 2 of story 914's 4 "revisit informational-only metrics" bullets: Concurrent LB3 targets gained a real judgement (good when 2+ targets sustained LB3's 3rd stack for at least 50% of the fight, otherwise unjudged — never fair/bad, since the right number depends on raid healing assignments this app can't see), and Nature's Swiftness gained a utilization judgement (good/fair/bad bands matching Swiftmend's own, with a 1-available-window exception for sub-3-minute fights where holding it in reserve is reasonable). Swiftmend itself gained a second judgement, utilization, alongside its existing wasteful-share judgement. A final whole-branch review caught that utilization's initial bands (copied from Nature's Swiftness's 75%/50%) were unreachable given Swiftmend's much shorter cooldown — story 909's real corpus averages only ~3.3 casts per fight — so they were softened to 50%/25% the same day, marked provisional in docs/thresholds.md pending real calibration data for this specific metric. Both Swiftmend judgements and Nature's Swiftness's now fold into Spell Discipline's epic verdict (gated by the same Restoration-point talent-eligibility thresholds story 903c already established); Concurrent LB3 targets folds into Lifebloom Discipline's. `scripts/lib/calibrateReport.ts`/`rollup.ts` were restructured to match — concurrent-targets and Nature's-Swiftness data moved out of a separate `informational`-only bag and into their real epics' metrics, gaining pooled `swiftmendUtilizationPctPooled`/`naturesSwiftnessUtilizationPctPooled` stats alongside the existing pooled figures. A drive-by fix in the same pass: `computeConsumableThroughput`'s own Mana-Potion/Rune row combination was using strict `worstJudgement` instead of `mixedJudgement`, the one remaining multi-part metric not following the established good+bad-reads-fair pattern — now consistent with the rest of the codebase. Story 914 is now fully done — its last open bullet, HoT-tick overheal (Rejuvenation and Regrowth's HoT portion), was confirmed 2026-07-21 to stay informational rather than gain a threshold. The whole-report dashboard's epic chip strip also gained an interactive judgement rollup breakdown, requested directly (no backlog story) — a "2 fair" style segment is now interactive whenever its chip has 2+ distinct judgement buckets present (a single-bucket breakdown like "12 good" stays plain text), opening a popover on hover/keyboard-focus/tap that lists exactly which fights are in that bucket and lets you click straight through to that fight's scorecard with the relevant epic focused. Implemented via a new generic, hand-rolled `Popover` primitive (`src/app/components/ui/Popover`, no new dependency) and a new `onOpenFightEpic` callback threaded from `App.tsx` down through `ReportDashboard`; `rollupEpicJudgement` (`src/metrics/reportAggregation.ts`) now returns each bucket's fight identities alongside its existing counts, and a shared `formatFightLabel` helper (`src/report/fightRows.ts`) keeps the popover's boss-name links and the fight-row labels byte-for-byte consistent. Pooled against the full local calibration corpus (`calibration-data/`, grown to 120 reports for this review — including 20 added from a second guild's self-labeled elite deep-resto healer, high WCL parses, specifically to stress-test the finding against a plausibly-skilled outlier), HoT-tick overheal shows real, wide, archetype-dependent spread (deep-resto median 54%/57%, dreamstate median 61%/64%, p10-p90 spans 35-45+ points) — nothing like Bloom overheal's tight 72-74% clustering that earned it a real threshold in story 905. The decisive check: for druids with 3+ fights, average within-player fight-to-fight spread exceeds average between-player spread on both spells, meaning the same druid swings more across their own fights than typical players differ from each other (confirmed even for the newly-added "elite" healer, whose own Rejuvenation overheal ranged 19-84% within one 13-fight report) — the signature of an encounter-driven number, not a stable per-player skill trait, so the original "inherent to HoTs during raid healing, don't judge" call was confirmed as-is with no code change. See `docs/thresholds.md`'s mana economy section for the full numeric findings. Story 710 (Judgement Rationale page, epic H) is done too — a new user-facing `#/judgements` page (MDX-authored, `src/app/components/JudgementRationale/content.mdx`, compiled to a real component at build time via `@mdx-js/rollup`) explains the process-over-output philosophy, how judgements combine, every metric's exact threshold (imported live from the same exported constants `docs/thresholds.md` already indexes, so the two can't drift apart), and a conceptual GraphQL data-sourcing overview; the existing Onboarding/About screen moved from `localStorage`-gated overlay state to a real `#/about` route in the same story, with a first-time visit anywhere now redirecting there and resuming wherever the visitor was actually headed once dismissed. `useHashRoute.ts` also now re-syncs on `hashchange` (not just `popstate`), which is what lets `MetricCard`'s new per-metric "read the full rationale" link use a plain anchor instead of threading a `navigate` callback through every metric card. Story 602 (enchant & gem check, epic G) is done too — it extends `computePrepHygiene` (`src/metrics/prepHygiene.ts`) with two new judged rows, `computeEnchantCoverage` (9 enchantable slots: Head, Shoulder, Back, Chest, Wrist, Hands, Legs, Feet, MainHand's _permanent_ enchant, distinct from 601's existing MainHand _temporary_ weapon-oil check at the same index) and `computeGemCoverage` (every socketed gem plus a Head-slot meta-gem check), both matched by numeric ID off the same `CombatantInfo` `gear` array 601/903a already read — no new WCL query. Recognition is tiered (`bis`/`acceptable`, the two members of the exported `GearTier` type, plus a third "missing" state represented structurally by the absence of a recognized id in the lookup tables rather than a value of `GearTier` itself) rather than a strict binary, because real gearing has legitimate lesser-but-real choices that a binary check would misjudge as gaps — the case that forced the design was the Aldor faction's shoulder enchant, where Honored-tier (`INSCRIPTION_OF_FAITH_ID`) is a genuine interim choice before reaching Exalted-tier (`GREATER_INSCRIPTION_OF_FAITH_ID`); `bis` and `acceptable` both count as fully covered, only `missing` (absent or unrecognized) counts against the bands. `judgeEnchantCoverage`: good 0 missing, fair 1-3, bad 4+ of the 9 slots; `judgeGemCoverage`: good 0 missing-or-wrong, fair 1-2, bad 3+, folding meta-gem recognition into the same count rather than a separate row (both bands are marked provisional in `docs/thresholds.md` pending a future calibration pass, the same posture Swiftmend utilization shipped with per story 802/909's note, since no exemplar corpus exists yet for this metric). `computePrepHygiene`'s `mixedJudgement` call grows from 3 to 5 inputs to fold the two new rows into Prep Hygiene's overall verdict, with no signature change. A documented, accepted limitation carries through from live research this story confirmed (`docs/testing.md`'s `4GYHZRdtL3bvhpc8` entry): WCL's `CombatantInfo` gear entries can't distinguish an unfilled socket from no socket at all (`gems` is either absent or non-empty, never `[]`), so gem coverage can only flag a present-but-wrong gem, never a genuinely empty one — the same limitation applies to the Head-slot meta-gem check, which reads "no gems at all on Head" identically to "wrong meta gem present." `src/testUtils/factories.ts`'s `aCombatantInfoEvent` default `gear` now carries recognized bis-tier `permanentEnchant`/`gems` values on top of its existing MainHand `temporaryEnchant`, so every pre-existing Prep Hygiene test that doesn't override `gear` exercises a fully-prepped combatant on the two new rows for free.
