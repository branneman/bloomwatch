# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Claude Design pass output (external tool, dropped in for reference during a
# design handoff, not hand-authored repo source) — see docs/backlog.md's notes
# on stories 009/706 for the pattern.
docs/design_v*

# superpowers:subagent-driven-development's progress ledger — local scratch
# state for an in-progress plan execution, not project content.
.superpowers/

# scripts/calibrate.ts output — local working cache for calibration passes
# (story 802/900-902). Durable findings get distilled into docs/testing.md's
# known-reports table and docs/thresholds.md instead; the raw per-report JSON
# is scratch material, not itself worth committing at the volume a real
# calibration pass generates. (One pre-existing committed file predates this
# policy — calibration-data/4GYHZRdtL3bvhpc8.json — left as historical, not
# retroactively removed.)
calibration-data/
